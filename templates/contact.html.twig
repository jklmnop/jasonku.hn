{{ form_start(form) }}
    <fieldset>
        <legend>
            <h2>What's on your mind?</h2>
        </legend>
        {{ form_widget(form) }}
        <a href="{{ path('app_spam') }}">Spamart</a>
    </fieldset>
{{ form_end(form) }}
